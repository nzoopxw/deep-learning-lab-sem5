# Lab 05 — CNN Training, Regularization, Optimization & Cross-Validation
# MobileNetV2 + Oxford-IIIT Pet (TensorFlow/Keras)
# Install with:  pip install -r requirements.txt

tensorflow>=2.12          # MobileNetV2 (keras.applications) + training
tensorflow-datasets>=4.9  # Oxford-IIIT Pet dataset loader
numpy>=1.24               # array handling, BN example, preprocessing
matplotlib>=3.7           # all 14 training/comparison plots
seaborn>=0.12             # confusion-matrix heatmap
scikit-learn>=1.3         # KFold cross-validation, precision/recall/F1, confusion_matrix

# For running the notebook (omit if you run as a plain .py script)
jupyter>=1.0
