# Lab 04 — Transfer Learning & CNN Architecture Comparison (TensorFlow/Keras)
# Install with:  pip install -r requirements.txt

tensorflow>=2.12         # VGG16/ResNet50/MobileNetV2 (keras.applications) + cifar10
numpy>=1.24              # array handling, shapes, preprocessing
matplotlib>=3.7          # sample grids, accuracy/loss curves, misclassified images
seaborn>=0.12            # confusion-matrix heatmap
scikit-learn>=1.3        # precision/recall/F1, confusion_matrix, classification_report

# For running the notebook (omit if you run as a plain .py script)
jupyter>=1.0
